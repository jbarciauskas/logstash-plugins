input {
  stdin {
    codec => "json"
  }
}
output {
  stdout {
    codec => "json"
  }
  mysql {
    username => "root"
    password => ""
    table_name => "test"
    insert_ignore => true
    column_map => ["foo", "foo", "bar", "bar", "timestamp", "@timestamp"]
    jdbc_connection_string => "jdbc:mysql://localhost:3306/test"
  }
}
